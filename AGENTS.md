# 다녀담 공개 개발 컨텍스트

`AGENTS.md`와 `CLAUDE.md`는 동일하게 유지한다. API와 DB의 현재 계약은
`API-CONTRACT.md`와 `docs/SCHEMA.md`, 제품 변경은 `docs/DECISIONS.md`를 기준으로 한다.

## 제품과 출시 목표

- 실제 장소 방문을 확인하고 여행 카드를 모으는 무료 iOS·Android 앱이다.
- 앱 ID는 `kr.danyeodam.app`, 지원 언어는 `ko`, `en`, `ja`, `zh-Hans`, `zh-Hant`, `vi`다.
- 결제, 광고, 카드 거래, 현금화, 랭킹, 배틀은 출시 범위에 없다.
- 공개 저장소에는 코드와 합성 테스트 데이터만 둔다. 비밀키, 원시 위치좌표, 실사용자 데이터,
  서명자료, 심사계정, 카드 권리자료, 현장승인자료, 스토어 증빙은 커밋하지 않는다.

## 기능 불변식

1. 원시 좌표는 저장하거나 로그에 남기지 않는다. 위치 확인은 foreground 요청에서만 수행한다.
2. 같은 사용자·장소의 현장 일반카드는 Asia/Seoul 날짜 기준 하루 한 번만 지급한다.
3. 방문 일반카드와 일일 보너스 카드는 서로 다른 원장에 저장한다. 보너스는 방문 통계를 늘리지 않는다.
4. 일일 보너스 팩은 계정당 Asia/Seoul 하루 한 개이며, 첫 유효 현장 방문으로만 지급한다.
5. 보너스 결과는 발급 트랜잭션에서 서버가 한 번 확정한다. 재시도·재설치·개봉 취소로 재추첨하지 않는다.
6. 목표 확률은 일반 80%, 특별 20%이며, 4회 연속 일반 후 다음 팩은 특별을 보장한다.
7. 팩은 만료되지 않는다. 중복은 수량으로 합치고, 거래·분해·재화 전환은 제공하지 않는다.
8. 특별 카드는 승인된 별도 TCG 프레임 자산만 사용하며, 자산과 발급 기능은 권리·6개 언어·현장·출시 승인이 끝날 때까지 feature flag로 닫는다.
9. 공개 사진은 정책 동의, 검수, 신고, 차단, 철회, 삭제 경계를 모두 통과해야 한다.
10. 계정 삭제 요청은 접근과 공유를 즉시 닫고, Storage·DB·Auth 정리를 계속 재시도한다.
11. 첫 출시 카드 개봉 연출은 모션 감소를 지원하는 시각 효과와 선택적 햅틱만 사용하며 BGM·음악은 포함하지 않는다.

## 공개 저장소 규칙

- `main` 직접 푸시 금지. 브랜치, 테스트, PR, 필수 체크를 거친다.
- `.env.example`에는 예시값만 둔다. 실제 값은 배포 플랫폼의 비공개 Secret으로 주입한다.
- production EAS 빌드는 저장소 밖의 `DANYEODAM_RELEASE_APPROVAL_FILE`과 정확한 Git SHA가
  일치해야 시작한다.
- 보안 문제는 공개 Issue 대신 `SECURITY.md` 절차로 신고한다.
- 기존 비공개 저장소 이력을 공개 저장소로 복제하지 않는다.

## 검증 기준

- 공개 경계: `npm run security:test-public && npm run security:check-public`
- 웹/API: `npm run verify:public-source`
- 모바일: `npm --prefix apps/mobile run check`
- DB 변경: fresh reset, pgTAP, 동시성, upgrade, E2E, lint/advisors를 함께 통과한다.
- 로컬 빌드 성공은 스토어 출시 완료가 아니다. 정식 서명 IPA/AAB, 실제 운영 환경, 실기기,
  권리·현장·번역·정책 승인과 스토어 콘솔 검증은 별도 출시 게이트다.
