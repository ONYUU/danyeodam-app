# 기여 안내

다녀담은 공개 소스 저장소에서 개발하지만 현재 오픈소스 라이선스를 부여하지 않는다.
외부 기여를 시작하기 전에 Issue에서 변경 범위와 권리관계를 먼저 확인한다.

## 변경 절차

1. `main`이 아닌 브랜치에서 작업한다.
2. 실제 비밀값·사용자 데이터·원시 좌표·카드 원본·서명자료·출시 증빙을 추가하지 않는다.
3. `npm run verify:public-source`를 실행한다.
4. 모바일 변경은 `npm --prefix apps/mobile run check`도 실행한다.
5. PR에 변경 목적, 테스트 결과, 개인정보·보안·접근성 영향을 기록한다.

보안 취약점은 공개 Issue에 쓰지 말고 `SECURITY.md` 절차를 따른다.
